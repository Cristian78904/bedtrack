upstream bedtrack {
    server bed-service-1:80;
    server bed-service-2:80;
    server bed-service-3:80;
}

server {
    listen 80;
    location / {
        proxy_pass http://bedtrack;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
